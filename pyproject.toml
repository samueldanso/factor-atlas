[project]
name = "factor-atlas"
version = "0.1.0"
description = "Agentic factor discovery for 24/7 tokenized stock markets"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "numpy>=2.4.6",
    "pandas>=3.0.5",
    "pydantic>=2.13.5",
    "pytest>=9.1.1",
    "ruff>=0.16.7",
]
